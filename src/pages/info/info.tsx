export const Info = () => {
  return (
    <div>
      <ul>
        <li>-minimum task length: 3 characters</li>
        <li>-an empty string is not allowed</li>
        <li>
          -rtk query / test project(without changing the status) the status changes locally
          <ul>
            <li>-name must be shorter than or equal to 30 characters</li>
          </ul>
        </li>
      </ul>
    </div>
  )
}
